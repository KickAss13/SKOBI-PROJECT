{{ form_start(form) }}
    {{ form_widget(form) }}

<div class="d-flex justify-content-center">
    <button class="btn btn-secondary">{{ button_label|default('Sauvegarder') }}</button>
    <button type="button" class="btn btn-secondary"><a href="{{ path('fournisseur_index') }}" class="text-light">Retour à l'index</a></button>
</div>

    <div>
        {{ fournisseurFeedback ?? "" }}
    </div>
{{ form_end(form) }}
