{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.imageUpload) }}
<div class="d-flex justify-content-center">
    <button class="btn btn-secondary">{{ button_label|default('Sauvegarder') }}</button>
    <button type="button" class="btn btn-secondary"><a href="{{ path('event_index') }}" class="text-light">Retour à l'index</a></button>
</div>


        {{ eventFeedback ?? "" }}
    
{{ form_end(form) }}
