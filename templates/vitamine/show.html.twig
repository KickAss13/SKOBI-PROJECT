{% extends 'template-admin.html.twig' %}

{% block title %}Profil apport nutritionnel{% endblock %}

{% block body %}

<br><br>

<div class="d-flex justify-content-center">
    <h1>Profil apport nutritionnel</h1>
</div>

<br><br>

<div class="container d-flex justify-content-center">
    <table class="table table-bordered table-hover">
        <tbody>
            <tr>
                <th class="bg-secondary text-white">Nom apport nutritionnel</th>
                <td>{{ vitamine.nom }}</td>
            </tr>
            <tr>
                <th class="bg-secondary text-white">ImageSrc</th>
                <td><img src="{{ vitamine.imageSrc }}"></td>
            </tr>
        </tbody>
    </table>
</div>

<div class="d-flex justify-content-center">
    <button type="button" class="btn btn-secondary"><a href="{{ path('vitamine_index') }}" class="text-light">Retour à l'index</a></button>
 
    <button type="button" class="btn btn-secondary"><a href="{{ path('vitamine_edit', {'id': vitamine.id}) }}" class="text-light">Modifier</a></button>

    {{ include('vitamine/_delete_form.html.twig') }}
</div>

{% endblock %}
